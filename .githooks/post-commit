#!/bin/sh
repo_root="$(git rev-parse --show-toplevel 2>/dev/null)" || exit 0
if [ -z "$repo_root" ]; then
  exit 0
fi
if [ "$CODEX_AUTO_RELEASE_RUNNING" = "1" ]; then
  exit 0
fi
powershell.exe -NoProfile -ExecutionPolicy Bypass -File "$repo_root/tools/auto_release_post_commit.ps1"
exit 0
